
export default function show() {
    console.log(`
        * Variables
          // It supports var, const and let are supported.
          let varName : [Type: string|number|boolean|any] = initializationValue;
    `);
}